services:
  app:
    container_name: neuromatrix3d
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - path: .env
        required: false
    ports:
      - "${MATRIX_PORT:-5000}:${MATRIX_PORT:-5000}"
    tty: true
    stdin_open: true
    mem_limit: 2g
